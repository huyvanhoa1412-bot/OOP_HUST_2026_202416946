JAVA is strictly a Pass-by-Value programming language. When passing an object (like DigitalVideoDisc), Java passes a copy of the reference to that object. The swap() method fails to swap two objects because it only swaps the copied references in the local scope. However, the changeTitle() method succeeds because it uses the copied reference to access and modify the actual attributes of the original object in the heap memory.